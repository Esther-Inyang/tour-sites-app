import React from "react";

function Loading() {
  const styles = {
    color: "rgb(5, 23, 77)",
    textAlign: "center",
    margin: "100px 0",
  };

  return (
    <div style={styles}>
      <h1>Loading...</h1>
    </div>
  );
}

export default Loading;
